public void NULL_DEREFERENCE(){
                // (and also, array[i] would have already appeared in one of LIS, identify the location and replace it)
            
                tail[upperBound(null, -1, length - 1, array[i])] = array[i];
        

}